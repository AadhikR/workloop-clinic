export default function ExpensesManager() {
  return (
    <section className="card">
      <h2>Expenses moved</h2>
      <p>Use the migration expense workspace for claim review, decisions, and receipts.</p>
    </section>
  );
}
