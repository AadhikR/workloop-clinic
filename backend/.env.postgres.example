POSTGRES_PASSWORD=replace-with-a-unique-local-secret
WORKLOOP_MIGRATION_PASSWORD=replace-with-a-different-local-secret
WORKLOOP_RUNTIME_PASSWORD=replace-with-a-different-local-secret
WORKLOOP_EXPIRY_PROCESSING_PASSWORD=replace-with-a-different-local-secret
KEYCLOAK_DB_PASSWORD=replace-with-a-different-local-secret
