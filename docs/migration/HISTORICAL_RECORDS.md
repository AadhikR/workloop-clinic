# Migration records through Phase 12

Historical record. Do not run or deploy these files.

Directories `phase-0/` through `phase-12/` record how Workloop moved away from Supabase. Commands,
paths, rollback notes, and service references in those directories describe the repository at the
time of each phase. They are evidence, not current setup or deployment instructions.

Use the root `README.md`, `ARCHITECTURE.md`, `DIGITALOCEAN_MIGRATION_PLAN.md`, and `CLAUDE.md` for
current instructions. Phase 13 records remain active migration evidence until the phase closes.
