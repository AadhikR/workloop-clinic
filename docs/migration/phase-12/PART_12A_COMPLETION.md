# Part 12A completion

Status: implementation and local gate complete on 2026-09-24. The routed GitHub result is the final
external gate and belongs in the Part 12B handoff rather than a second evidence-only commit.

## Result

Part 12A established the Phase 12 boundary without changing runtime behavior. The inventory assigns
97 dependencies to Parts 12B through 12G or Phase 13 and traces 52 upstream assignments exactly
once. The contract fixes authorization, response shapes, filters, ordering, pagination, Dubai
business dates, decimal rounding, deterministic bytes, filenames, encodings, download headers,
idempotency, errors, audit, and reverse rollback.

The 50 golden cases cover recipient and branch isolation, expiry thresholds and concurrency,
complete task categories, snapshot dashboards, authoritative reports, CSV and SIF bytes,
deterministic PDF and ZIP output, protected audit, and rollback.

## Decisions

- Phase 12 consumes only authoritative Phase 7 through 11 projections. It does not repair or mutate
  source-domain state.
- Expiry production is an explicit command under the existing `workloop_expiry_processing` login.
  No scheduler is needed or authorized.
- Existing notification schema, deduplication, RLS, grants, and expiry audit linkage support 12B.
- No new table or column is required. Parts 12F and 12G need one fixed-purpose protected audit
  writer because the existing generic writer does not allow output-generation actions.
- SIF preview parses the exact generated bytes. Print opens an authorized PDF. Neither has a second
  browser renderer.
- Generated bytes are streamed and discarded. The database retains safe audit metadata only.
- Phase 13 retains ownership of legacy Supabase removal and migration-build promotion.

## Verification

The focused verifier passed with 97 catalogue entries, 52 upstream assignments, and 50 golden
cases. Changed-file UTF-8, trailing-whitespace, and final-newline validation passed. `git diff
--check` passed before the untracked deliverables were staged.

The required final local gate passed:

- `python scripts/verify-phase-12a-contract.py`
- `npm.cmd run test:unit`, 261 passed and 0 failed
- `npm.cmd run build`, 2,052 modules transformed and the production build completed

Vite retained its existing chunk-size warning. Part 12A did not change application code, so the
warning is not a regression.

## Resource boundary

No runtime route, schema revision, UI cutover, database row, service, credential, cloud resource,
paid resource, or production record changed. The work used repository files and synthetic contract
fixtures only. The protected `workloop-clinic_postgres_data` volume was not attached, modified,
deleted, or recreated.

## Continuation

After the routed Migration foundation jobs pass, Part 12B starts automatically in a new Codex task
in the same saved project and local checkout. Part 12B implements notifications and explicit expiry
production only. It does not begin task aggregation.
